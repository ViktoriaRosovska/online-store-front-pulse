// export const countPrice = (items, countQuantity = 0) => {

//   items?.reduce((acc, el) => {
//     if (el) {
//       acc += el.price * el.quantity;
//       countQuantity += el.quantity;
//     }

//     return acc;
//   }, 0);
// };
